Using a command line, navigate to the project directory.  
From the root, run these commands:

javac -d bin/ src/*.java

cd bin

java src.Main


The output file is "out.txt" and is in the form <time stamp> <name>:  <action>
